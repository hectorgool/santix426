
<% if user_signed_in? %>
  	<div class="col-md-4 col-md-offset-4">
  		<div class="page-header">
		    <h2>
		        New Tour
		    </h2>
	    </div>
	    <%= render "form", tour: @tour %>
  	</div>
<% else %>

<% end %>