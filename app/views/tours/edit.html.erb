<% if user_signed_in? %>
  	<div class="col-md-6 col-md-offset-3">
  		<div class="page-header">
		    <h2>
		        <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>Edit Tour
		    </h2>
	    </div>
	    <%= render "form", tour: @tour %>
  	</div>
<% else %>

<% end %>