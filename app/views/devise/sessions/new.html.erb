<div class="container">
  <div class="col-md-6 col-md-offset-3">
    <div class="panel panel-default">
      <div class="panel-body">
      
        <div class="page-header">
          <h1>Log in</h1>
        </div>

        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

          <%= devise_error_messages! %>
          <% flash.each do |key, value| %>
              <div class="alert alert-danger flash <%= key %>" role="alert">
                <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
                <%= value %> 
               </div>
          <% end %>
          
          <%= f.input :email, autofocus: true, class: "form-control input-lg" %>
        
          <%= f.input :password, autocomplete: "off", class: "form-control input-lg" %>
          
          <% if devise_mapping.rememberable? -%>
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me %>
          <% end -%>

<<<<<<< HEAD
          <%= f.button :submit, "Log in", class: "btn btn-black btn-lg btn-block" %>
=======
          <%= f.button :submit, "Log in", class: "btn btn-blue btn-lg btn-block" %>
>>>>>>> tours

        <% end %>

        <%= render "devise/shared/links" %>

        <p class="lead text-center">OR</p>

        <%= render "social/links" %>        

      </div>
    </div>
  </div>
</div>