<%= simple_form_for [tour, comment] do |f| %>

  <%= f.input :text,
    as: :text,
    label: false,
    class: 'form-control input-lg',
    rows: '100'
  %>

  <%= f.button :submit, class: 'btn btn-orange btn-lg btn-block' %>

<% end %>
